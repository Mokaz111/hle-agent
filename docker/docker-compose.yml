version: '3.8'

services:
  # 主应用服务
  hle-agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: hle-agent
    image: hle-agent:latest
    restart: unless-stopped
    volumes:
      - ../config.yaml:/app/config.yaml:ro
      - ../data:/app/data
      - ../logs:/app/logs
      - ../results:/app/results
    environment:
      - TZ=Asia/Shanghai
    networks:
      - hle-network
    depends_on:
      - python-executor
      - sagemath-executor
    # 如果需要 HTTP API，取消下面的注释
    # ports:
    #   - "8080:8080"

  # Python 执行器服务（隔离环境）
  python-executor:
    build:
      context: ..
      dockerfile: docker/python_executor/Dockerfile
    container_name: python-executor
    image: hle-agent-python:latest
    restart: unless-stopped
    # 资源限制
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    # 安全配置
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
    networks:
      - hle-network
    # 健康检查
    healthcheck:
      test: ["CMD", "python3", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  # SageMath 执行器服务（隔离环境）
  sagemath-executor:
    build:
      context: ..
      dockerfile: docker/sagemath_executor/Dockerfile
    container_name: sagemath-executor
    image: hle-agent-sagemath:latest
    restart: unless-stopped
    # 资源限制（SageMath 需要更多资源）
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 1G
    # 安全配置
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=200m
    networks:
      - hle-network
    # 健康检查
    healthcheck:
      test: ["CMD", "sage", "-c", "1+1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  hle-network:
    driver: bridge

