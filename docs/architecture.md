# AI Agent 总体架构设计文档 - HLE 考试答题系统

## 文档信息

| 项目 | 内容 |
|------|------|
| **项目名称** | HLE 考试答题智能体系统 |
| **关联文档** | 需求文档 V1.0 |
| **版本** | V2.0 |
| **文档状态** | 待评审 |
| **创建日期** | 2026-01-05 |
| **技术框架** | CloudWeGo Eino ADK |

---

## 一、架构设计概述

### 1.1 设计理念

本架构设计基于 **CloudWeGo Eino ADK** 的 **Plan-Execute Agent** 模式进行设计，充分利用框架提供的"规划-执行-反思"范式，构建专注于 HLE 考试答题的专用智能体系统。

架构设计的核心目标是实现智能性与可控性的平衡。在智能性方面，系统需要具备理解复杂题目、进行多步推理、执行计算任务、生成准确答案的能力；在可控性方面，系统需要确保每一步决策都有明确的依据和日志记录，核心高风险操作需要经过验证，同时系统需要具备异常处理和自我纠错能力。

### 1.2 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          人机交互层 (Human-Interaction Layer)                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ 结果展示    │  │ 过程可视化  │  │ 交互配置    │  │ 审核模式    │          │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘          │
├─────────────────────────────────────────────────────────────────────────────┤
│                            反馈层 (Feedback Layer)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ 答案校验    │  │ 置信度评估  │  │ 质量评估    │  │ 经验沉淀    │          │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                    Eino ADK PlanExecuteAgent 核心                            │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │                         Planner (规划器)                              │     │
│  │  - 理解题目意图                                                      │     │
│  │  - 生成解题计划                                                      │     │
│  │  - 定义执行步骤                                                      │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                       │                                      │
│                                       ▼                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │                        Executor (执行器)                              │     │
│  │  - 执行解题步骤                                                      │     │
│  │  - 调用外部工具                                                      │     │
│  │  - 返回执行结果                                                      │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                       │                                      │
│                                       ▼                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │                        Replanner (重规划器)                           │     │
│  │  - 评估执行结果                                                      │     │
│  │  - 决定是否需要重规划                                                │     │
│  │  - 生成新计划或结束任务                                              │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                          执行层 (Execution Layer)                            │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐   │
│  │     Python执行器    │  │    SageMath执行器   │  │     知识检索器      │   │
│  │    (MCP Tool)       │  │    (MCP Tool)       │  │    (Eino Tool)      │   │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────────┤
│                          记忆层 (Memory Layer)                               │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐   │
│  │     短期记忆        │  │     长期记忆        │  │     经验知识库      │   │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────────┤
│                          感知层 (Perception Layer)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ 数据解析器  │  │ 领域识别器  │  │ 特征提取器  │  │ 格式规范化  │          │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 Eino ADK Plan-Execute 模式

**Plan-Execute Agent** 是 Eino ADK 中一种基于"规划-执行-反思"范式的多智能体协作框架，包含三个核心智能体：

| 组件 | 职责 | 在本系统中的作用 |
|------|------|------------------|
| **Planner** | 生成解题计划 | 理解题目，生成结构化解题步骤 |
| **Executor** | 执行计划步骤 | 调用工具，执行推理，返回结果 |
| **Replanner** | 评估并调整计划 | 评估结果，决定是否需要重规划 |

**工作流程：**
```
1. 用户输入题目
2. Planner: 生成解题计划（调用LLM + PlanTool）
3. Executor: 执行每个计划步骤（调用Tools）
4. Replanner: 评估结果，决定是否需要重新规划
5. 回到步骤2 或 返回结果
```

### 1.4 架构设计原则

本架构设计遵循以下核心原则：

**目标唯一性原则**：整个系统围绕"HLE 考试答题"这一单一、明确的目标进行设计，不试图构建一个能够处理各种类型任务的通用系统。

**最小自主性原则**：自主性是一把双刃剑，自主性越强，系统智能化程度越高，但可控性越差。本架构将系统的自主权限严格限制在"低风险、高确定性"的环节，而将"高风险、高不确定性"的环节交给规则驱动或多模型校验机制。

**可解释性原则**：系统的每一个决策必须有明确的依据。感知层负责提取输入特征并记录原始数据，中枢大脑在做出决策时必须生成决策依据日志，执行层的每个操作都有完整的参数记录和结果反馈。

**鲁棒性原则**：确保系统能够应对各种异常情况。工具调用可能失败、模型返回可能异常、输入数据可能不完整——本架构在每个层级都设置了相应的容错机制。

**效率优先原则**：推理规划不追求"最优解"，而是追求"可行解"；任务拆解不追求"极致细粒度"，而是追求"能执行、无歧义"。

---

## 二、Eino ADK 技术架构

### 2.1 Eino ADK 组件集成

Eino ADK 提供了丰富的组件集成能力，本系统使用的核心组件包括：

| 组件类型 | 组件名称 | 用途 |
|----------|----------|------|
| ChatModel | OpenAI/Claude | LLM调用 |
| Tool | Python Execute | Python代码执行 |
| Tool | SageMath Execute | 数学计算 |
| Tool | Retriever | 知识检索 |
| ADK | PlanExecuteAgent | 多智能体协作 |
| Callback | Langfuse | 可观测性 |

### 2.2 Plan-Execute Agent 实现

```go
// 使用Eino ADK创建PlanExecuteAgent
agent, err := planexecute.NewAgent(
    planexecute.AgentConfig{
        Name:        "HLE-Agent",
        Description: "HLE考试答题智能体",
    },
    planner,    // 自定义Planner
    executor,   // 自定义Executor
    replanner,  // 自定义Replanner
)
```

### 2.3 工具集成架构

```
Eino ADK Tools
├── Built-in Tools
│   ├── SequentialThinking - 顺序思考工具
│   ├── Calculator - 计算器
│   └── HTTPRequest - HTTP请求
│
├── Custom Tools (MCP)
│   ├── PythonExecutor - Python代码执行
│   │   └── 实现：通过HTTP调用Python MCP服务
│   │
│   └── SageMathExecutor - SageMath数学计算
│       └── 实现：Docker容器化部署
│
└── Knowledge Tools
    └── Retriever - 知识检索
        └── 实现：Eino Retriever接口
```

---

## 三、核心组件设计

### 3.1 Planner（规划器）

Planner 负责理解题目并生成解题计划，是系统的"大脑"入口。

**核心功能：**
- 理解题目意图，提取关键信息
- 根据题目类型选择合适的解题策略
- 生成结构化的解题计划（Step + Action）

**Prompt设计：**
```text
你是一个专业的HLE考试答题助手。请分析以下题目并制定解题计划。

题目：{question}

请制定一个详细的解题计划，包括：
1. 理解题目要求
2. 识别关键信息
3. 选择解题策略
4. 执行计算或推理
5. 验证答案

请按JSON格式输出计划...
```

### 3.2 Executor（执行器）

Executor 负责执行Planner生成的每个步骤，是系统的"手"。

**核心功能：**
- 根据Step的Action类型执行相应操作
- 调用外部工具（Python、SageMath等）
- 返回执行结果

**执行流程：**
```
Step.Action = "llm" → 调用LLM进行推理
Step.Action = "tool" → 调用对应Tool
Step.Action = "calculate" → 执行计算
```

### 3.3 Replanner（重规划器）

Replanner 负责评估执行结果并决定是否需要重规划，是系统的"反思"机制。

**核心功能：**
- 分析执行结果，判断是否成功
- 决定是否需要重规划
- 生成新的计划或结束任务

**重规划策略：**
- 所有步骤成功 → 结束任务，返回结果
- 超过一半步骤失败 → 重新生成计划
- 最后一步失败 → 重试当前步骤

---

## 四、数据流设计

### 4.1 整体数据流

```
用户输入题目
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                    Eino ADK PlanExecuteAgent                  │
│                                                              │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐                  │
│  │ Planner │───▶│ Executor│───▶│Replanner│                  │
│  └─────────┘    └─────────┘    └─────────┘                  │
│       │              │              │                        │
│       ▼              ▼              ▼                        │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐                  │
│  │ Plan    │    │ Result  │    │ Plan/   │                  │
│  │ 生成    │    │ 执行    │    │ End     │                  │
│  └─────────┘    └─────────┘    └─────────┘                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
         │                    │
         ▼                    ▼
    记忆层存储           工具层调用
    反馈层校验           输出结果
```

### 4.2 状态流转

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Pending │───▶│Executing │───▶│ Evaluating│───▶│Completed │
│  (待执行) │    │ (执行中) │    │ (评估中) │    │ (已完成) │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                       │
                                       │ Replan
                                       ▼
                                  ┌──────────┐
                                  │ Replanning│
                                  │ (重规划中) │
                                  └──────────┘
```

---

## 五、技术栈总结

### 5.1 核心技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| **开发语言** | Go | Agent主体开发 |
| **Agent框架** | CloudWeGo Eino ADK | Plan-Execute Agent |
| **工具语言** | Python | MCP Tools实现 |
| **LLM** | OpenAI/Claude | 推理与规划 |
| **容器化** | Docker | SageMath环境 |
| **配置管理** | YAML | 配置文件 |

### 5.2 框架优势

使用Eino ADK Plan-Execute Agent模式的优势：

1. **减少重复工作**：无需自己实现Planner、Executor、Replanner
2. **标准化接口**：遵循Eino ADK的设计规范
3. **工具集成**：统一管理所有外部工具
4. **容错机制**：内置重试、回滚机制
5. **可观测性**：完整的Callback支持

---

## 六、安全与可靠性

### 6.1 安全措施

| 措施 | 说明 |
|------|------|
| 代码沙箱 | Python代码在隔离环境中执行 |
| 资源限制 | CPU、内存、超时限制 |
| 输入验证 | 题目数据格式验证 |
| 错误处理 | 完整的异常捕获与恢复 |

### 6.2 可靠性保障

| 保障措施 | 说明 |
|----------|------|
| 重试机制 | 失败步骤自动重试 |
| 回滚机制 | 支持回滚到指定步骤 |
| 日志记录 | 全链路可追溯 |
| 降级策略 | 工具不可用时的备选方案 |

---

## 七、总结

本架构设计基于 CloudWeGo Eino ADK 的 Plan-Execute Agent 模式，具有以下特点：

1. **框架驱动**：利用Eino ADK预构建框架，减少重复开发
2. **层次清晰**：感知、记忆、规划、执行、反馈，职责分明
3. **工具集成**：统一管理Python、SageMath等外部工具
4. **可扩展性**：支持添加新的工具和策略
5. **可控性强**：完整的容错和重试机制

---

**文档版本**：V2.0
**更新说明**：基于Eino ADK Plan-Execute Agent模式重构架构设计
